---
title: __TITLE__
author: __AUTHOR__
digest:
---

# __TITLE__

在这里粘贴原始内容、整理稿，或者你希望我改写后的版本。

现在的默认约定是：

- 只维护 `article.md` 这一份正文源文件，发布前会自动同步标题、摘要、模板 HTML 和 `preview.html`
- 配图统一放进 `assets/`，正文里优先继续使用稳定槽位名，如 `![配图 1](img-01.jpg)`
- 提示词统一写进 `generated/image-prompts.md`
- 推草稿统一走仓库根目录的 `scripts/publish_wechat_article.ps1`

如果这篇文章会直接生图，提示词必须写详细，至少包含：

- 画面主体与场景
- 文章里的核心数据、时间、人物、研究或结构
- 中文文案要求：清晰、可读、无乱码、无错字
- 负面提示词：不要乱码、不要错字、不要英文字母占位

